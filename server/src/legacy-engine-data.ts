// The vendored engine still fixes this compatibility directory name. Keep the
// literal centralized until its paths package makes the name configurable.
export const LEGACY_ENGINE_DATA_DIRECTORY = ".archon";
