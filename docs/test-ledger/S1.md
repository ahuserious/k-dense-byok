# S1 test-ledger entry (web)

The Phase R web baseline is 590 passed / 0 skipped. S1 intentionally removes
the typed visual builder, canvas, and inspector together with their obsolete
same-basename tests. After replacement coverage for consolidation, legacy-view
migration, the vendored-only Builder surface, complete typed-definition
inspection, and safeguarded typed Run admission, the orchestrator count is
571 passed / 0 skipped.

<!-- test-ledger-entry {"id":"s1-typed-builder-surface-removal","suite":"web","kind":"delta","active":true,"passedDelta":-19,"skippedDelta":0,"reason":"S1 deleted 19 net tests with the removed typed builder, canvas, and inspector surfaces after accounting for replacement consolidation, persistence, inspection, and safeguarded Run tests."} -->

Deleted test files with their production surfaces:

- web/src/components/dag-builder.test.tsx
- web/src/components/dag-builder-canvas.test.tsx
- web/src/components/dag-builder-inspector.test.tsx

The structured delta is net `-19`, not a claim that these three files contained
only 19 individual cases. It reconciles the removed cases against the new and
extended S1 tests already present in the 571-test observation.

Server count is unchanged by S1 because this lane does not edit the typed
runtime.
